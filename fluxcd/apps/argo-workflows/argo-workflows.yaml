apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: argo-workflows
  namespace: argo
spec:
  interval: 5m
  chart:
    spec:
      chart: argo-workflows
      version: "0.*.*"
      sourceRef:
        kind: HelmRepository
        name: argo-charts
        namespace: flux-system
