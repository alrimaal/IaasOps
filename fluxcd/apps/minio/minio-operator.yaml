apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio-operator
  namespace: minio
spec:
  interval: 5m
  chart:
    spec:
      chart: operator
      version: "7.1.*"
      sourceRef:
        kind: HelmRepository
        name: minio-charts
        namespace: flux-system
