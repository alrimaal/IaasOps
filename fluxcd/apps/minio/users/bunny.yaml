apiVersion: bfiola.dev/v1
kind: MinioUser
metadata:
  name: minio-bunny-user
  namespace: minio
spec:
  accessKey: Rockband1768
  secretKeyRef:
    name: minio-bunny-user-secrets
    key: secretKey
  tenantRef:
    namespace: minio-tenant
    name: myminio
