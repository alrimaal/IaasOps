apiVersion: bfiola.dev/v1
kind: MinioUser
metadata:
  name: minio-loki-user
  namespace: minio
spec:
  accessKey: ScalableLoki
  secretKeyRef:
    name: loki-secrets
    key: LOKI_MINIO_PASSWORD
  tenantRef:
    namespace: minio-tenant
    name: myminio
