apiVersion: bfiola.dev/v1
kind: MinioUser
metadata:
  name: minio-bunny-prod-user
  namespace: minio
spec:
  accessKey: Ludicrous9628
  secretKeyRef:
    name: minio-bunny-prod-user-secrets
    key: secretKey
  tenantRef:
    namespace: minio-tenant
    name: myminio
