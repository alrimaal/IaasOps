apiVersion: bfiola.dev/v1
kind: MinioUser
metadata:
  name: minio-argo-dev-user
  namespace: minio
spec:
  accessKey: Relight5991
  secretKeyRef:
    name: minio-argo-dev-user-secrets
    key: secretKey
  tenantRef:
    namespace: minio-tenant
    name: myminio
