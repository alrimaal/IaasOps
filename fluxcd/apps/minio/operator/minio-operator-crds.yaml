apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio-operator-crds
  namespace: minio
spec:
  interval: 5m
  chart:
    spec:
      chart: crds
      version: "3.3.*"
      sourceRef:
        kind: HelmRepository
        name: minio-operator-ext-charts
        namespace: flux-system
