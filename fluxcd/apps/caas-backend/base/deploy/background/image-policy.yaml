apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: caas-background
  namespace: default
spec:
  imageRepositoryRef:
    name: caas-background
  policy:
    semver:
      range: x.x.x
