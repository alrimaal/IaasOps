apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: caas-backend
  namespace: default
spec:
  imageRepositoryRef:
    name: caas-backend
  policy:
    semver:
      range: 0.x.x
