# app-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: caas-background
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: caas-background
          envFrom:
            - configMapRef:
                name: caas-config
            - secretRef:
                name: caas-secrets
