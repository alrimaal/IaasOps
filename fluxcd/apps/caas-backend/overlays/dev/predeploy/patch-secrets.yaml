apiVersion: batch/v1
kind: Job
metadata:
  name: caas-db-migration
  namespace: default
spec:
  template:
    spec:
      containers:
        - name: caas-db-migration
          envFrom:
            - configMapRef:
                name: caas-config
            - secretRef:
                name: caas-dev-secrets
