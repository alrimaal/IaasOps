apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: muzbuild-backend
  namespace: default
spec:
  imageRepositoryRef:
    name: muzbuild-backend
  policy:
    semver:
      range: x.x.x
