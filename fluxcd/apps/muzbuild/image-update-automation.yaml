apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: muzbuild-backend
  namespace: default
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: muzbuild-backend
  update:
    strategy: Setters
