apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: muzbuild-backend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: muzbuild-backend
  policy:
    semver:
      range: 0.x.x
