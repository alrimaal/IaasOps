apiVersion: v1
kind: Service
metadata:
  name: muzbuild-svc
  namespace: default
spec:
  selector:
    app: muzbuild-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7000
  type: ClusterIP
