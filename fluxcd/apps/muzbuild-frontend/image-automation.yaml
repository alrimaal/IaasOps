apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: muzbuild-frontend
  namespace: default
spec:
  interval: 1m
  image: ghcr.io/alrimaal/muzbuild-frontend
  secretRef:
    name: ghcr-secret
