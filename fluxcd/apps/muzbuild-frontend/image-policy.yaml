apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: muzbuild-frontend
  namespace: default
spec:
  imageRepositoryRef:
    name: muzbuild-frontend
  policy:
    semver:
      range: x.x.x
