apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: qirtaas-frontend
  namespace: default
spec:
  imageRepositoryRef:
    name: qirtaas-frontend
  policy:
    semver:
      range: x.x.x
