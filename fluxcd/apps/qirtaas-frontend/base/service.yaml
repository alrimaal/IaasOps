apiVersion: v1
kind: Service
metadata:
  name: qirtaas-frontend
  namespace: default
spec:
  selector:
    app: qirtaas-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
