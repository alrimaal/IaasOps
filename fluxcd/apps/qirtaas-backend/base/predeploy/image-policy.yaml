apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: qirtaas-backend
  namespace: default
spec:
  imageRepositoryRef:
    name: qirtaas-backend
  policy:
    semver:
      range: x.x.x
