apiVersion: v1
kind: Service
metadata:
  name: caas-frontend
  namespace: default
spec:
  selector:
    app: caas-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
