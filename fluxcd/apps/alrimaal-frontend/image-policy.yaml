apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: alrimaal-frontend
  namespace: default
spec:
  imageRepositoryRef:
    name: alrimaal-frontend
  policy:
    semver:
      range: x.x.x
