apiVersion: v1
kind: Service
metadata:
  name: alrimaal-frontend
  namespace: default
spec:
  selector:
    app: alrimaal-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
