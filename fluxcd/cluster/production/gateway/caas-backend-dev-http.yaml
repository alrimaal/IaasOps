apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: caas-backend-dev-route
  namespace: dev
spec:
  parentRefs:
    - name: default-gateway
      namespace: cilium-gateway
      sectionName: https
  hostnames:
    - "bunyaandev.alrimaal.com"
  rules:
    - backendRefs:
        - name: caas-backend
          namespace: dev
          port: 80
